import { Groups } from "./src/index.js";
//import { GroupStage, Teams2 } from "./src/grupnaFaza.js";
import { sesiri, GroupStageStat } from "./src/Zreb.js";
import { quarterfinals,semifinals,finalsM } from "./src/EliminacionaFaza.js";



//GroupStage(Groups)


/*import readline from 'readline';

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question('Želite li da nastavite? (y/n) > ', (answer) => {
  if (answer.toLowerCase() === 'y') {
    console.clear()
    console.log("Nastavljamo sa izvršavanjem koda...");
    // Nastavak koda ovde
    
  } else {
    console.log("Izvršavanje koda je zaustavljeno.");
  }
  rl.close();
});*/
